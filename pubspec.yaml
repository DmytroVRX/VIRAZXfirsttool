name: virazx_quick_note
description: A new Flutter project.

environment:
  sdk: '>=3.4.0 <4.0.0' # мінімальні вимоги

dependencies:
  flutter:
    sdk: flutter
  bitsdojo_window: ^0.1.5
  shared_preferences: ^2.3.5

flutter:
  uses-material-design: true
  assets:
    - assets/images/